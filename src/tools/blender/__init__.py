"""
Blender-specific adapters.

Current implementation still lives in `src/planner`, `src/agents`, `src/bridge`,
and `src/loops`. This package exists as a namespace for future refactors and
tool-agnostic routing.
"""

